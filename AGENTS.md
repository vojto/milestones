# Agent Instructions

Your responses should be very brief, as if you're texting me while busy. Expand only if asked.

This repository contains two independent projects:

- `app/`: the Milestones macOS app. Follow `app/AGENTS.md` for app work and run its commands from `app/`.
- `website/`: the Milestones landing page.

Keep project-specific dependencies, tooling, and build output inside the relevant project folder.
